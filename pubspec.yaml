name: admin_scaffold
description: Professional reusable admin panel scaffold for Flutter with collapsible sidebar, breadcrumbs, and fully customizable options. Zero dependencies, production-ready.
version: 1.0.0
homepage: https://github.com/Abdelrahmanyehia9/admin_scaffold.git
repository: https://github.com/Abdelrahmanyehia9/admin_scaffold.git


environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

